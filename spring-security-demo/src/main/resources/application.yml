spring:
  datasource:
    url: jdbc:mysql:///sb-security?characterEncoding=UTF-8&useUnicode=true&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
mybatis-plus:
  global-config:
    # 关闭MP3.0自带的banner
    banner: false